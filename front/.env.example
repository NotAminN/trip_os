# Frontend environment
# Leave empty to use same-origin "/api" (recommended when served behind the
# same server/proxy as the backend). Set to an absolute URL only when the API
# lives on a different origin, e.g. https://api.your-domain.com/api
VITE_API_URL=
